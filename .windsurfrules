# Windsurf Rules: ChatExport Need Miner

Autonomous Market Signal & Customer Need Extractor for offline Telegram Desktop `result.json` exports.

1. Bounded memory: Never exceed 8MB RAM. Use chunked streaming (4MB chunk + 4KB overlap).
2. Boundary protection: Match terminus must exceed overlap length to prevent double counts.
3. Multiplicity scoring: Prioritize issues seen across 3+ independent chats (Score = U * sqrt(H)).
4. Verbatim quotes: Mandatory 2-5 exact quotes with ISO timestamp per theme. Zero paraphrase.
5. Air-gap: Strictly offline. No live bot or network API requests.
